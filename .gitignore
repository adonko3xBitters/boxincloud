# Build artifacts
/bin/
/dist/
*.exe

# Le web compilé est embarqué dans le binaire ; seul le placeholder est versionné.
# Le web compilé est un artefact : il se régénère par `make build-web` et son
# index.html référence des fichiers au nom haché qui, eux, ne sont pas versionnés
# — le committer livrerait une coquille pointant sur des ressources absentes.
# Seul le répertoire est conservé, pour que `//go:embed` compile sans build web.
apps/server/web/dist/*
!apps/server/web/dist/.gitkeep

# Env
.env
.env.local
*.local

# Go
*.test
*.out
/coverage.*

# Node
node_modules/
.next/
out/
*.tsbuildinfo

# Flutter / Dart
.dart_tool/
.packages
build/
*.g.dart.bak

# Editors
.idea/
.vscode/
*.swp
.DS_Store

# Local data — cache d'images et fichiers de travail, tous régénérables.
# Deux emplacements parce que le répertoire de cache est relatif au dossier de
# lancement : à la racine en usage normal, sous apps/server quand on lance le
# serveur depuis son propre module. Les chemins sont énumérés plutôt qu'une
# règle « data/ » globale, qui avalerait un jour la couche data de l'app Flutter.
/data/
/apps/server/data/
/tmp/
